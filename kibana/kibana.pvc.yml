apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kibana-pv-claim
  namespace: es
  labels:
    app: kibana
    zone: sandbox
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: .5Gi